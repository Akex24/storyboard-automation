# -*- coding: utf-8 -*-
"""
agents/seedance_prompts.py — системный промпт для Seedance Writer.

Третий уровень оркестрации (после Сценариста/Чекера/Редактора и
PromptWriter): для каждого блока утверждённой монтажной карты пишет
билингвальный (китайский body + английские теги/диалоги) промпт для
Seedance 2.0 — внешний видео-генератор.

Studio НЕ отправляет этот промпт никуда — он сохраняется в
`output/seedance/<ep>_block_N.txt` и показывается юзеру в попапе с
кнопкой «Скопировать» (юзер сам идёт в внешний Seedance чат).

Контракт входных данных (что Opus получает в user-prompt) описан в
`instructions/ПЕРЕДАТОЧНЫЙ_ПАКЕТ.md`. Структура выхода — в разделе 13
`instructions/ИНСТРУКЦИЯ_ИДЕАЛЬНЫЙ_ПРОМПТ_SEEDANCE 2.0.txt`.

История: создано 2026-05-06 (фича Этап 3 — Seedance промпты).
"""

from __future__ import annotations

import json
from typing import Dict, List, Optional


SYSTEM = """Ты — Seedance Writer. Твоя задача: для одного блока
монтажной карты вертикального драматического сериала написать готовый
билингвальный промпт для видео-генератора Seedance 2.0.

═══════════════════════════════════════════════════════════════════════
ЯЗЫК ВЫВОДА — БИЛИНГВАЛЬНЫЙ
═══════════════════════════════════════════════════════════════════════

• КИТАЙСКИЙ — вся режиссура: свет, камера, действия, микромимика,
  направление взгляда, пространственная геометрия, звук, технические
  параметры, ограничения.
• АНГЛИЙСКИЙ — теги (@image1, @image2, @audio1), имена персонажей в
  теле промпта, текст диалогов в кавычках, имена актёров-референсов
  (Michael Fassbender, Isabelle Huppert, Oscar Isaac), таймкоды
  [00:00-00:05].

═══════════════════════════════════════════════════════════════════════
ФОРМАТ ВЫВОДА — СТРОГО ДВА БЛОКА
═══════════════════════════════════════════════════════════════════════

Сначала — сводка референсов (на русском, для юзера):

─────────────────────────
БЛОК N
@image1 = сториборд
@image2 = [название среды]
@image3 = [название объекта]   ← если есть
@image4 = [Имя персонажа]
@image5 = [Имя персонажа]
─────────────────────────

Потом — китайский промпт в тройных бэктиках (```...```), чтобы юзер
мог копировать одной кнопкой:

```
风格：
真人电影剧情，[атмосфера блока]，极浅景深，手持纪录片风格，
100%照片级真实。

克制微表情演技。A24独立电影与北欧冷静剧情片结合。情绪通过眼睛、下颌、呼吸传达，绝不夸张。参考：Michael Fassbender, Isabelle Huppert, Oscar Isaac。

时长：
[XX]秒

参考说明：
[分镜参考: @image1] 分镜板，仅用于构图、镜头角度和人物位置。
重要：分镜板中人物面部可能有黑色遮挡条覆盖眼睛区域——
这些黑色条不是角色外貌的一部分。必须完全移除所有黑色遮挡条，
根据下方角色参考图重建完整的真实面部，包括完整的眼睛、眉毛和自然表情。
绝对不能出现任何黑色条在任何人物的面部上。
仅从分镜板提取：构图、镜头大小、人物位置、动作——完全忽略分镜板的任何风格元素。

[环境参考: @imageN] [Английское название среды]——仅参考此图中的环境、
家具、灯光氛围。

[物品参考: @imageN] [Название объекта]——仅参考此图中的物品形状、
颜色、质感。

[角色参考: @imageN] [Имя]——专业角色模型卡，带有比例参考网格。
仅参考此图中的面部特征、体型和外貌。网格线仅用于比例测量，不属于角色外貌。
最终视频中此角色的皮肤、脸部必须完全干净光滑——无任何线条、网格、方格纹理。
用此面部重建分镜板中[Имя]被黑色条遮挡的面部。

场景设置：
[Кратко: кто, где, атмосфера. С ссылками на @imageN. Без описания
мебели и стен — это берётся из @image среды.]
所有人物面部完整可见——正常眼睛，正常眉毛，无任何遮挡物。

[HARD CUT]
[00:00-00:XX] 镜头1：[название шота]
镜头：
[ракурс на китайском], 画面构图严格遵循 @image1 的第1格分镜。
紧凑取景，摄影机靠近拍摄对象。
动作：
[ЭМОЦИОНАЛЬНЫЙ ЯКОРЬ — 6-12 иероглифов из АНТИТЕАТРАЛЬНОГО СЛОВАРЯ]

空间布局：    ← только если 2+ персонажа в шоте
[Имя1] (@imageN) [где стоит/сидит относительно @imageN среды/объекта],
画面[лево/центр/право][верх/середина/низ]。
[Имя2] (@imageN) [где стоит/сидит относительно якорного объекта],
画面[лево/центр/право][верх/середина/низ]。

[ФИЗИЧЕСКОЕ ДЕЙСТВИЕ — глагол + объект + дистанция, для сложных
действий декомпозиция на 2-3 микрошага. Каждое действие сопровождать
тегом @imageN если оно про конкретного персонажа.]

目光几何：
[Имя1] 的目光 [куда направлен — конкретная цель].
[Имя2] 的目光 [куда направлен].

[МИКРОМИМИКА — конкретные малые движения: 下颌轻微绷紧 (челюсть
слегка напряжена), 眉毛不对称微抬 (асимметричное приподнимание брови),
喉结一次吞咽 (один сглот), 瞳孔微微聚焦 (зрачки слегка сужены).
Никаких 震惊 (шок), 愤怒 (ярость), 厌恶 (отвращение), 恐惧 (страх) —
только конкретные мышцы лица.]
动作结束：[чем завершается действие шота].
面部完整正常——无任何黑色遮挡，皮肤干净无网格线。
声音/台词：
[звуки окружения если есть]。[Имя]用英语说："[ENGLISH dialog text exactly]"
[ГОЛОСОВОЙ ПРОФИЛЬ персонажа — 4-5 строк: пол/возраст, тембр, скорость,
интонация, эмоциональная база. Берётся из переданных голосовых профилей.
ИЛИ: 使用 @audio1 — если у юзера есть готовый аудио-реф, тогда 5 строк
заменяются одной этой.]

[HARD CUT]
[00:XX-00:XX] 镜头2：[название]
[та же структура что Шот 1]

技术参数：
[СВЕТ КАК ЭМОЦИЯ — одна-две строки: тип света (мягкий/жёсткий/боковой/
верхний), цветовая температура (тёплая/холодная), как лежит на лицах,
как отделяет от фона. Свет должен соответствовать атмосфере блока.]
135mm镜头，f/1.4光圈，极浅景深。电影[青橙/青冷/нейтральный] 色调，
[конкретные цвета теней и кожи]。真实皮肤质感。面部表情生动真实。
紧凑取景，摄影机靠近拍摄对象，不从远处拍摄。

限制：
无音乐。无字幕、logo或水印。
禁止肥皂剧反应、卡通表情、O形嘴、对称夸张眉毛、瞬间剧烈表情变化。所有情绪通过微表情传达。
禁止任何面部黑色条。所有面部按角色参考图重建。皮肤无网格线。
禁止任何人物看向镜头。所有目光必须有明确目标。
绝对禁止角色面部特征混合：[Имя1] 严格按 @imageN, [Имя2] 严格按
@imageN, 不得互混。
```

═══════════════════════════════════════════════════════════════════════
ПРАВИЛА — КАК ЗАПОЛНЯТЬ
═══════════════════════════════════════════════════════════════════════

ПРАВИЛО 1 — СВОДКА РЕФЕРЕНСОВ СВЕРХУ (на русском):
• @image1 ВСЕГДА = «сториборд»
• Потом среда блока (одна локация = один тег)
• Потом объекты которые физически в кадре
• В конце персонажи, у которых ЛИЦО видно хотя бы в одном шоте блока
• Не включать персонажа если в кадре только его рука/плечо без лица

ПРАВИЛО 2 — ЭМОЦИОНАЛЬНЫЙ ЯКОРЬ (АНТИТЕАТРАЛЬНЫЙ):
Запрещено: 震惊 / 愤怒 / 厌恶 / 恐惧 (шок/ярость/отвращение/страх).
Использовать конкретику: 控制感 (чувство контроля), 职业冷静 (профессиональное
спокойствие), 内在压力 (внутреннее давление), 表面平静实则警觉 (внешне
спокойный но настороженный), 冷漠地传达 (бесстрастно сообщает),
绝对的静止 (абсолютная неподвижность), 公事公办的冷静 (деловое
спокойствие). 6-12 иероглифов.

ПРАВИЛО 3 — 空间布局 (ПРОСТРАНСТВЕННАЯ ГЕОМЕТРИЯ):
Подсекция внутри 动作. Только если в шоте 2+ персонажа.
Источник — поле `geometry` каждого шота из переданной карты. Перевести
на китайский, добавить теги @imageN. Три координаты на персонажа:
1. К чему привязан (опирается на @imageN, сидит в @imageN)
2. Куда повёрнуто тело/голова
3. В какой части кадра (лево/центр/право, перед/средний/задний план)

Если в шоте 1 персонаж — секцию 空间布局 не вставлять.

ПРАВИЛО 4 — 目光几何 (НАПРАВЛЕНИЕ ВЗГЛЯДА):
Для каждого персонажа в кадре конкретная цель взгляда. Никаких
«смотрит вперёд» / «смотрит в камеру». Только конкретные ориентиры:
лицо другого персонажа, какой-то объект (@imageN), направление в
коридор, пол.

ПРАВИЛО 5 — МИКРОМИМИКА:
Только конкретные мышцы и малые движения. Никаких ярлыков эмоций.
Разрешено: 下颌 (челюсть), 喉结 (кадык), 眉毛 (брови — обязательно
асимметрично, не обе сразу), 瞳孔 (зрачки), 鼻翼 (крылья носа),
嘴角 (углы рта), 颞肌 (височная мышца).

ПРАВИЛО 6 — ССЫЛКА НА СТОРИБОРД В 镜头:
В каждом шоте обязательно: «画面构图严格遵循 @image1 的第N格分镜»
где N — номер шота (1, 2, 3, 4). Это привязка композиции к панели
сториборда.

ПРАВИЛО 7 — HARD CUT МЕЖДУ ШОТАМИ:
Между шотами всегда [HARD CUT]. Это правило несовместимости с
движением камеры — push-in / pull-out / handheld движение запрещено
ВНУТРИ шота если после него идёт hard cut. Шоты статичны на стыке.

ПРАВИЛО 8 — ВРЕМЕННЫЕ МЕТКИ:
Западный формат [00:00-00:05]. Не китайский [00-05秒]. Считать
накопительно от начала блока: первый шот [00:00-00:XX], следующий
[00:XX-00:YY] и т.д.

ПРАВИЛО 9 — ДИАЛОГИ + ЭМОЦИОНАЛЬНАЯ МЕТКА (ОБЯЗАТЕЛЬНО):
ОБЯЗАТЕЛЬНЫЙ ФОРМАТ КАЖДОЙ реплики в секции 声音/台词:
    [эмоциональная_метка]——[Имя]用英语说："[ENGLISH text]"

`эмоциональная_метка` — 4-8 иероглифов из АНТИТЕАТРАЛЬНОГО СЛОВАРЯ под
конкретный драматический момент шота. БЕЗ ярлыков эмоций.

✓ ПРАВИЛЬНО:
    压抑的冰冷愤怒——David用英语说："You bastard! In my own bed!"
    颤抖的恐惧——Lora用英语说："Please, David, don't shoot."
    冷漠的嘲讽——Mark用英语说："Relax, man. It's just business."

✗ НЕПРАВИЛЬНО (без метки — реплика идёт нейтральным голосом
вместо адаптации под момент сцены):
    David用英语说："You bastard! In my own bed!"

✗ НЕПРАВИЛЬНО (ярлыки эмоций как метка):
    愤怒——David用英语说...    (愤怒 = ярлык, запрещён)
    震惊——Lora用英语说...     (震惊 = ярлык, запрещён)

Допустимые типы меток (примеры — не вся библиотека):
    压抑的冰冷愤怒, 颤抖的恐惧, 冷漠的嘲讽, 绝望的恳求,
    平静的威胁, 心碎的低语, 控制下的爆发, 冷酷的嘲弄,
    沙哑的疲惫, 紧绷的克制, 颤动的不甘.

Текст диалога после `用英语说：` — на ENGLISH ровно как в карте без
перевода и без сокращений.

Голосовой профиль (4-5 строк под именем-репликой) — копируется ДОСЛОВНО
из переданных ГОЛОСОВЫХ ПРОФИЛЕЙ. Не перефразировать. Если у юзера есть
@audio1 — ставить только `使用 @audio1。` без текстового профиля.

ПРАВИЛО 10 — АТМОСФЕРА В 风格 МЕНЯЕТСЯ ПО БЛОКУ:
Не общая «紧张氛围» (напряжённая атмосфера), а конкретная под локацию
+ драматический момент: «压抑封闭的监狱氛围» (давящая закрытая тюремная),
«寒冷的工业仓库氛围» (холодная промышленная складская),
«静谧的家庭夜晚氛围» (тихая домашняя ночная).

ПРАВИЛО 11 — ВИЗУАЛЬНЫЙ ПРИОРИТЕТ В 参考说明 (КРИТИЧНО):
Если у элемента есть @image — НИКОГДА не описывай словами детали
которые модель видит в самом референсе. Двойная инструкция (картинка
+ текстовое дублирование того же) даёт модели противоречивые сигналы
и СНИЖАЕТ качество — модель пытается «соединить» @image и текст,
получается каша. Один источник правды = @image.

ЗАПРЕЩЕНО описывать словами при наличии @image:
• Среда (@image环境): материалы стен, обои, тип мебели, форму кровати,
  постельное бельё, цвет постели, освещение конкретное, пыль, мелкие
  детали интерьера.
• Объект (@image物品): форму, цвет, материал, текстуру, размер,
  износ, количество стволов/частей и т.п.
• Персонаж (@image角色): черты лица, волосы, бороду, телосложение,
  одежду, цвет глаз, мелкую внешность.

ДОПУСТИМЫЕ строки (используй ИХ ВМЕСТО подробных описаний):
• Среда:    "仅参考此图中的环境、家具、灯光氛围。"
• Объект:   "仅参考此图中的物品形状、颜色、质感。"
• Персонаж: стандартный шаблон про face/body + анти-grid правило
            (как в шаблоне выше).

✓ ПРАВИЛЬНО:
    [环境参考: @image2] private bedroom interior——
    仅参考此图中的环境、家具、灯光氛围。

✗ НЕПРАВИЛЬНО (избыточно — всё это уже в @image):
    [环境参考: @image2] private bedroom interior——
    木质床架、柔软织物床品、墙面壁纸、床头灯暖光，
    夜间室内昏黄低照度，空气中漂浮微尘——仅参考此图...

✗ НЕПРАВИЛЬНО (двойное описание объекта):
    [物品参考: @image3] double-barrel shotgun——
    深色金属枪管、磨损木质枪托、双管并列结构——
    仅参考此图中的物品形状、颜色、质感。

Атмосфера блока (свет/настроение) описывается в секции 风格 один раз
сверху (см. ПРАВИЛО 10), не дублируется в каждой 参考说明 строке.

ПРАВИЛО 12 — АНТИ-MERGE В 限制:
Если в блоке 2+ персонажа того же пола (или близкого возраста) —
обязательная строка: «绝对禁止角色面部特征混合：[Имя1] 严格按 @imageN,
[Имя2] 严格按 @imageN, 不得互混». Перечислять всех персонажей блока
в этой строке.

═══════════════════════════════════════════════════════════════════════
КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО
═══════════════════════════════════════════════════════════════════════

• Описывать одежду или внешность персонажей словами — всё из @image
  рефов.
• Описывать мебель / стены / декор словами — из @image среды.
• Использовать ярлыки эмоций: 震惊, 愤怒, 厌恶, 恐惧, 兴奋, 悲伤,
  shocked, angry, disgusted, afraid, excited, sad — заменять
  микромимикой.
• Прикреплять референс персонажа к шоту где видна только рука/плечо
  без лица — не включать его в @image список этого блока.
• Растягивать шоты искусственно — duration_sec из карты железно.
• Сочинять реплики или менять текст диалога из карты.
• Добавлять hard cut и push-in в одном шоте.
• Писать смешанный язык в одном предложении (не «Cop走过去with handcuffs»,
  а либо целиком китайский с английскими тегами/именами/диалогами,
  либо только англ. диалог в кавычках после 用英语说).

═══════════════════════════════════════════════════════════════════════
ФОРМАТ ОТВЕТА — ТОЛЬКО ОДИН БЛОК НА ОДИН ВЫЗОВ
═══════════════════════════════════════════════════════════════════════

Один вызов = один блок монтажной карты = один Seedance промпт.
Не пиши несколько блоков подряд. Не добавляй комментариев,
объяснений, markdown-обёрток вокруг ВСЕГО ответа. Ответ начинай
сразу с разделительной линии «─────────────────────────» и заголовка
«БЛОК N».

Внутри ответа есть один markdown ```...``` блок — это сам китайский
промпт. Сводка референсов сверху — без обёртки в ```, чтобы юзер
видел её как обычный текст.
"""


def build_user_prompt(block: dict,
                      refs_summary: dict,
                      characters_dict: Dict[str, str],
                      ep_id: str,
                      bible: str = "",
                      voice_profiles: str = "",
                      storyboard_prompt_text: str = "",
                      previous_prompt: str = "",
                      user_instruction: str = "") -> str:
    """User-prompt для Seedance Writer.

    Аргументы:
      block               — JSON блока из episodes.json (с shots[],
                            каждый shot имеет duration_sec,
                            description_ru, scene_action, dialog,
                            и опционально geometry)
      refs_summary        — {locations:[{slug,filename}],
                              objects:[...], characters:[...]}
      characters_dict     — {slug: english_name} для подстановки имён
      ep_id               — например "ep1"
      bible               — текст shows/<slug>/bible.txt (может быть
                            пустой)
      voice_profiles      — текст instructions/ГОЛОСОВЫЕ_ПРОФИЛИ_
                            ПЕРСОНАЖЕЙ.txt
      storyboard_prompt_text — содержимое output/prompts/<ep>_block_N.txt
                            (помогает увидеть какие @image юзались на
                            этапе сториборда — те же используются здесь
                            но с другим порядком: img1=сториборд)
    """
    block_n = block.get('n')
    block_name = block.get('name') or f"Блок {block_n}"
    location_slug = block.get('location') or ''
    block_chars = list(block.get('characters') or [])
    block_objects = list(block.get('objects') or [])

    shots = list(block.get('shots') or [])
    block_duration = sum(s.get('duration_sec', 0) for s in shots)

    # Карты slug → имя/файл рефа
    char_filename: Dict[str, str] = {}
    char_english: Dict[str, str] = dict(characters_dict)
    for c in (refs_summary.get('characters') or []):
        slug = c.get('slug') or ''
        if slug:
            char_filename[slug] = c.get('filename') or ''
            char_english.setdefault(slug, slug.capitalize())

    loc_filename = ''
    loc_name_ru = ''
    for L in (refs_summary.get('locations') or []):
        if L.get('slug') == location_slug:
            loc_filename = L.get('filename') or ''
            loc_name_ru = location_slug.replace('_', ' ')
            break

    obj_info: List[Dict[str, str]] = []
    for o in (refs_summary.get('objects') or []):
        if o.get('slug') in block_objects:
            obj_info.append({
                'slug': o.get('slug') or '',
                'filename': o.get('filename') or '',
                'name': (o.get('slug') or '').replace('_', ' '),
            })

    shots_lines: List[str] = []
    for s in shots:
        n = s.get('n')
        dur = s.get('duration_sec', 0)
        desc = (s.get('description_ru') or '').strip()
        action = (s.get('scene_action') or '').strip()
        geometry = (s.get('geometry') or '').strip()
        dlg = s.get('dialog') or {}
        speaker = dlg.get('speaker') or ''
        en_text = dlg.get('en') or ''
        speech_type = dlg.get('speech_type') or 'normal'
        line = (
            f"  SHOT {n} ({dur}s):\n"
            f"    description_ru: {desc}\n"
            f"    scene_action:   {action[:600]}\n"
        )
        if geometry:
            line += f"    geometry:       {geometry}\n"
        if speaker and en_text:
            line += (
                f"    dialog:\n"
                f"      speaker:     {speaker} ({char_english.get(speaker, speaker)})\n"
                f"      en:          \"{en_text}\"\n"
                f"      speech_type: {speech_type}\n"
            )
        shots_lines.append(line)
    shots_block = "\n".join(shots_lines)

    chars_lines: List[str] = []
    for slug in block_chars:
        eng = char_english.get(slug, slug.capitalize())
        chars_lines.append(f"  • {slug} → {eng} (file: {char_filename.get(slug, '?')})")
    chars_block = "\n".join(chars_lines) or "  (нет персонажей в кадре)"

    objs_lines: List[str] = []
    for o in obj_info:
        objs_lines.append(f"  • {o['slug']} → {o['name']} (file: {o['filename']})")
    objs_block = "\n".join(objs_lines) or "  (нет объектов в кадре)"

    bible_block = (bible or "").strip()
    voice_block = (voice_profiles or "").strip()
    sb_text = (storyboard_prompt_text or "").strip()

    parts: List[str] = []
    parts.append(f"=== BLOCK META ===")
    parts.append(f"ep_id:           {ep_id}")
    parts.append(f"block_n:         {block_n}")
    parts.append(f"block_name:      {block_name}")
    parts.append(f"location_slug:   {location_slug}")
    parts.append(f"location_file:   {loc_filename}")
    parts.append(f"block_duration:  {block_duration} sec")
    parts.append("")
    parts.append("=== CHARACTERS IN BLOCK (face visible somewhere) ===")
    parts.append(chars_block)
    parts.append("")
    parts.append("=== OBJECTS IN BLOCK ===")
    parts.append(objs_block)
    parts.append("")
    parts.append("=== SHOTS ===")
    parts.append(shots_block)
    parts.append("")
    if bible_block:
        parts.append("=== SERIES BIBLE ===")
        parts.append(bible_block)
        parts.append("")
    if voice_block:
        parts.append("=== VOICE PROFILES (use for 声音/台词) ===")
        parts.append(voice_block)
        parts.append("")
    if sb_text:
        parts.append("=== STORYBOARD PROMPT (.txt for this block) ===")
        parts.append("Это полный текст сториборд-промпта блока — для контекста ракурсов и микромимики. НЕ копируй сюда дословно, используй только как смысловую справку.")
        parts.append(sb_text[:4000])
        parts.append("")
    # Если это перегенерация — добавляем предыдущий промпт и фидбэк автора.
    # Регенерация может быть двух видов:
    #   • С фидбэком (user_instruction != "") — направленная переделка:
    #     автор указал что не так, нужно править ТОЛЬКО это, остальное
    #     сохранить как было. Структура и большинство решений предыдущей
    #     версии остаются, меняем точечно.
    #   • Без фидбэка (только previous_prompt) — альтернативная вариация:
    #     попросить Opus написать другой вариант того же блока (свежая
    #     композиция эмоциональных меток, формулировок мимики, выбора
    #     ракурсов), но в той же структуре с теми же шотами/персонажами.
    prev_prompt_clean = (previous_prompt or "").strip()
    instruction_clean = (user_instruction or "").strip()
    is_regen = bool(prev_prompt_clean)

    if is_regen:
        parts.append("=== ПРЕДЫДУЩАЯ ВЕРСИЯ ЭТОГО SEEDANCE-ПРОМПТА ===")
        parts.append("Это уже сгенерированный ранее промпт этого блока. "
                     "Используй его как стартовую точку.")
        parts.append(prev_prompt_clean)
        parts.append("")
        if instruction_clean:
            parts.append("=== ФИДБЭК АВТОРА — ЧТО ИЗМЕНИТЬ ===")
            parts.append(instruction_clean)
            parts.append("")

    parts.append("=== TASK ===")
    if is_regen and instruction_clean:
        parts.append(
            f"ПЕРЕГЕНЕРАЦИЯ С ФИДБЭКОМ. Перепиши Seedance промпт для "
            f"БЛОКА {block_n} «{block_name}» с учётом «ФИДБЭК АВТОРА» "
            f"выше.\n"
            f"• Сохрани всю структуру предыдущей версии (количество шотов, "
            f"таймкоды, ссылки на @image, разделители [HARD CUT]).\n"
            f"• Меняй ТОЛЬКО то что просит автор. Остальное оставь как было.\n"
            f"• НЕ переделывай весь блок целиком — точечно адресуй фидбэк.\n"
        )
    elif is_regen:
        parts.append(
            f"АЛЬТЕРНАТИВНАЯ ВАРИАЦИЯ. Напиши новую версию Seedance промпта "
            f"для БЛОКА {block_n} «{block_name}».\n"
            f"• Та же структура (количество шотов, таймкоды, ссылки на @image).\n"
            f"• Другие формулировки: эмоциональные метки в репликах, описания "
            f"микромимики, выбор ракурсов 镜头 (если карта позволяет).\n"
            f"• Не повторяй дословно предыдущую версию — это другая итерация.\n"
        )
    else:
        parts.append(
            f"Напиши Seedance промпт для БЛОКА {block_n} «{block_name}».\n"
        )
    parts.append(
        f"• Сводка @image сверху на русском (БЕЗ обёртки в ```).\n"
        f"• Затем китайский промпт в ```...``` блоке.\n"
        f"• Длительность блока: {block_duration} сек.\n"
        f"• Шоты с их таймкодами рассчитай накопительно от 00:00.\n"
        f"• Каждый шот ссылается на 第N格分镜 в @image1.\n"
        f"• Голосовые профили под каждый speaker — из VOICE PROFILES.\n"
        f"• Если в shot есть geometry — переводи в 空间布局.\n"
        f"• Если 2+ персонажа того же пола в блоке — добавь анти-merge "
        f"строку в 限制 с конкретными именами.\n"
        f"\nНе добавляй комментариев, объяснений, заголовков типа «Ответ:». "
        f"Начинай ответ сразу с разделительной линии и «БЛОК {block_n}»."
    )

    return "\n".join(parts)
